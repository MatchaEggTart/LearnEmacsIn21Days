# 组合键提示器

* Which-key

  * HOW

      ~/.config/emacs/lisp/init-package.el

      ``` lisp
      ;; 组合键提示器
      (use-package which-key
        :defer nil
        :config (which-key-mode))
      ```
